{
  "name": "claws-formal",
  "version": "1.0.0",
  "description": "CLAWS Formal Event App",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "vercel-build": "mkdir -p public/data && cp -R data/* public/data/"
  },
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.6.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
